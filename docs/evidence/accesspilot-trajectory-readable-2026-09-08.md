# 轨迹可理解性局部验收

2026-09-08，轻量前端交付；不改后端、业务合同或模型调用，不操作用户演练中的会话。

## 改动

- 标题根据已记录意图、目录匹配结果及前后缺失字段命名，例如查询可申请权限、选择申请权限、补充期限/理由、确认申请信息。证据不足时回退为用户原话摘要，不调用模型命名。
- 每轮展开先看用户原话、处理结果和模型次数；随机编号及详细步骤移到“技术详情”，“最新”仅作标签。
- 现有 `/api/events?follow=false` 已按当前 Workspace 返回完整事件；前端每次增加三轮，按已有事件顺序展开历史，并按事件 ID 去重。未新增历史 API。
- 正式业务操作单列。仅用当前会话已出现的 submitted request_id，通过已有鉴权申请详情 API 读取创建、冻结材料与审批事实。该区不会把聊天确认当作提交，不把冻结操作计为聊天 0 次；材料调用次数显示未记录。读取失败明确展示，旧请求不会混入新会话的展示。

## 本轮证据

- 定向前端测试 20 项全部通过（AgentTrajectory 9、TrajectoryBusinessOperations 2、App 9），覆盖原有计量、去重、更多历史、目的标题、身份作用域内请求 ID、业务读取失败与确认边界。
- ESLint、TypeScript、Vite 构建通过。构建仍有原有超过 500 kB 的包体提示。
- ego-browser 独立空间 31 使用虚构连续五轮 fixture 及受控只读申请详情响应；从最近三轮实际点击展开到五轮，标题、确认输入/结果、聊天 0 次与业务材料未记录已目视验证。
- 桌面与 390px 手机视口均无横向溢出。截图为本文件同目录的 `accesspilot-trajectory-readable-desktop-2026-09-08.png`、`accesspilot-trajectory-business-2026-09-08.png`、`accesspilot-trajectory-readable-mobile-2026-09-08.png`。
- 临时 fixture HTML 已删除，独立浏览器空间已关闭。5173 开发服务持续运行，刷新即可加载新前端，无需重启后端或清数据。

限制：本轮浏览器验证是 fixture 展示验证，没有替用户进行正式提交、生成材料、审批或发放。标题依据现有安全事件；缺少历史字段变化时会保留原话摘要。业务材料计量未接入，本次不扩展为账单工程。没有 commit/push/deploy。
