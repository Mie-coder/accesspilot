# 决策材料分层与建议矛盾修复

2026-09-08，轻量局部交付。保留已有混合路由、低成本和轨迹功能；不改审批规则或冻结记录。

## 实际变化

- 页面按申请概况 → 审批依据 → AI 审查建议排列。理由明确标申请人填写及真实性待核对；目录事实和固定路线独立展示。
- 仅对已知 fictional-catalog-v1 / policy v1 合同归类：通用完整性/禁止自审批；高风险双审批需要冻结目录策略及路线共同支持；客户导出条款限对应权限。其他条款及未知版本折叠为检索参考，保留原文与来源，要求核对范围。
- Packet/Catalog/mode 移入技术详情。冻结数据没有具体提供方标识，因此 provider 仅标“外部模型生成的建议”，不猜测成 DeepSeek；离线与不可用模式保留真实名称。
- 旧建议中“身份/具体权限缺失”等可识别的矛盾保留原文并显示冲突提示，明确以上方已验证事实为准、没有重新生成。
- 新建议上下文增加 requester_verified / entitlement_verified 两个布尔事实，姓名、员工编号、权限编码仍不外传。提示词区分脱敏省略与业务缺失、申请人陈述与已核实事实、检索参考与实际适用。对已复现的否认已验证字段表述增加拒绝校验，失败仍保留事实和固定路线，建议标不可用。

## 验证

- 先复现缺少存在性字段与矛盾建议被接受的 3 个失败测试，再最小修复。
- 后端定向 31 项通过、0 跳过：建议适配器、冻结生成与重复/并发生成、ACL、只读查询、不重新咨询模型的审批边界。
- 前端受影响组件 40 项通过；补充已知/未知政策版本测试后材料组件 7 项通过。
- Ruff、MyPy、ESLint、TypeScript/Vite 构建通过，只有原有 >500 kB 包体提示。
- 1 次真实 deepseek-v4-flash 调用，使用虚构“仪表盘查看、7天、客户演示”上下文，不持久化申请/材料。返回明确承认身份及权限已核验，建议引用 POL-001/POL-006，未套用高风险双审批；用途真实性留给人工核对。见 accesspilot-packet-advisory-live-2026-09-08.json。未测 token；单次结果不构成对所有自然语言输出的保证。
- ego-browser 独立空间 32 用 fixture 验证旧矛盾展示、参考政策实际展开及技术折叠；1100px 桌面和 390px 窄屏无横向溢出，截图已目视核对。截图为 accesspilot-packet-layout-desktop-2026-09-08.png / accesspilot-packet-layout-mobile-2026-09-08.png。

## 可复测状态与限制

本地服务已通过 start 保留数据重载，5173 刷新可看新排版。旧正式申请的 frozen_content 未改写、未删除、未重算；要观察新的生成建议，需要后续新申请生成自己的新材料，不能覆盖既有材料。本轮没有操作用户正在演练的会话，没有代为提交/审批/开通，没有 commit/push/deploy。临时 fixture HTML 已删除、独立浏览器空间已关闭。

## 结论优先文案补充

同日按用户最新要求，仅改前端展示。AI 建议先显示结论，再列依据和去重后的核对事项。复用冻结 advisory 的 assessment/summary/unknowns/recommendations：assessment 不是审批建议，不机械映射；只有明确建议通过且 clear、无未知项/补充前提时才标建议通过，明确否决意见且无相反意见才标建议不通过，明确要求补充材料才标建议补充材料后再审，其余保守显示暂无法给出可靠建议。

旧冲突展示具体列出已核验申请人和权限、已记录期限、目录风险等级，并说明无需按旧建议重复补充；原文及未知项/建议默认折叠到“查看原始历史建议”。保留原始冻结记录，正常无冲突建议不折叠。

材料组件 13 项通过，TypeScript 和对应 ESLint 通过。ego 独立 fixture 验证结论在先、历史原文默认关闭，截图 accesspilot-packet-conclusion-2026-09-08.png；空间 33 与临时 HTML 已清理。无后端或模型合同变化，无模型调用，5173 刷新可见。现有建议没有独立的审批倾向字段；没有明确、无冲突的表态时不会推断“建议通过”。

### 新生成建议的结论格式

继续补充生成端 prompt，保持原有五字段 schema：summary 主动以四类建议之一开头，随后给具体事实/政策依据；信息不足必须保守，不能机械把风险等级当审批意见。常规人工核对不等于缺材料，不能因此伪称申请字段缺失；建议通过须无待补项/未解决风险。旧 Packet 不重算。

定向建议适配器 9 项通过，Ruff 通过。另做 1 次真实 deepseek-v4-flash 虚构上下文调用（未持久化 Packet、未测 token），返回“建议通过。本次申请字段完整，授权期限7天未超过180天上限，风险等级为low且适用manager审批，符合POL-001要求。”，assessment=clear、unknowns=[]，后续事项为人工核验用途及身份分离。符合现有前端的“建议通过”判定，同时不是实际批准。证据 accesspilot-packet-conclusion-live-2026-09-08.json。与此前旧历史 fixture 的“暂无法给出可靠建议、原文折叠”分开验证；不能把一次模型输出推广为稳定性保证。
