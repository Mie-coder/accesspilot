# AccessPilot T15 最终验收交叉评审记录

**评审日期：** 2026-08-10
**评审类型：** `final-acceptance`
**范围：** T15 政策问答、证据状态、自审批规则、复合提示词攻击防护和敏感信息边界
**权限：** 外部模型与独立 reviewer 只读；不得修改文件、提交、推送或部署

## 1. 匿名化验收包

### 目标

- 基本政策问题返回完整的 8 条主题目录。
- 具体政策问答只使用当前轮检索证据，并稳定返回 `grounded`、`insufficient_evidence` 或 `retrieval_unavailable`。
- 自审批问题明确拒绝并引用 `POL-006`；申请、草稿、审批和开通状态仍由确定性服务决定。
- 申请与提示词索取、政策与密钥索取、自审批与身份覆盖、有效意图与未知工具参数等复合消息不泄露内部内容、不采用攻击参数、不越过写操作守卫。
- 模型输出、草稿、事件、SSE 增量和错误正文不包含系统提示词、密钥、令牌、隐藏推理或数据库凭据。

### 非目标

- 不接入真实 SSO、IAM 或企业政策数据。
- 不评审 T16 业务卡片或 T17 产品化评测。
- 外部模型只接收去除源码、密钥、私有地址和数据库凭据的行为摘要与测试证据，不获得写权限。

### 验收证据

- T15 专项：33 tests passed。
- 后端全量：300 tests passed，1 warning。
- Ruff、MyPy、AST 语法检查和 `git diff --check` 通过。
- 独立只读验收 reviewer 多轮复核，最终无残留 P0/P1。

## 2. 评审模型状态

### Claude Code Sonnet 5

基于匿名化验收包完成只读评审，返回 `no_p0_p1`、`confidence=medium`。其余建议属于证据可追溯性和边界覆盖的非阻塞改进，已由最终专项计数和回归证据补齐。

### DeepSeek

T15 专项验收包未发送，原因是缺少针对 T15 的明确外发授权。本记录不将其表述为模型不可用，也不把未发送视为通过或失败结论。此前其他 Ticket 的授权与评审记录不自动延伸到 T15。

## 3. 内部独立评审与修复记录

### T15-IR-01 — P1 — 已修复

结构化模型输出可能把系统提示词写入理由字段。修复为在持久化草稿、完成事件和对外回答前统一清理内部提示词标记，并为模型返回的提示词内容保留安全空值。

### T15-IR-02 — P1 — 已修复

模型返回的 `confirmed=true` 可能绕过用户明确确认。修复为确认状态只由服务端根据原始用户消息中的批准短语决定；安全探针不会被模型字段提升为确认。

### T15-IR-03 — P1 — 已修复

常见“我能不能自己批准自己的申请”等自审批表达未稳定路由到禁止规则。修复共享自审批分类器，使服务、路由和工具保持同一确定性结果，并固定引用 `POL-006`。

### T15-IR-04 — P1 — 已修复

复合攻击中的提示词、密钥、未知工具参数和身份覆盖标记覆盖不足。修复安全标记识别、严格工具参数校验和业务意图/安全意图分离，避免攻击参数进入写操作。

### T15-IR-05 — P1 — 已修复

流式回答完整泄露或逐字符泄露系统提示词的风险。修复累计内容检查和前缀缓冲：可疑提示词前缀不会先作为增量发送；完整、逐字符和截断前缀均以安全可重试终态结束，不持久化泄露内容。

## 4. 结论

T15 的专项功能、后端回归、静态检查和独立只读验收均达到当前 Ticket 的二元验收标准；Claude 匿名化只读评审无 P0/P1。DeepSeek 本轮未发送，原因仅为缺少 T15 专项外发授权，不能外推为模型状态或评审结论。T15 结论为：**可进入本地提交，不代表推送、合并、部署或最终发布。**
